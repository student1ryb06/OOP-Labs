# Лабораторна робота №2
## З дисципліни «Об’єктно-орієнтоване програмування»

**Тема:** «Використання бібліотеки NumPy для роботи з матрицями»

**Виконала:** студентка групи КН-31з
Рибка Л.Г.

**Перевірив:** 
Татомир А.В.

Львів 2025

---

## Мета
Вивчення основ роботи з багатовимірними масивами (матрицями) за допомогою бібліотеки `NumPy`.

## Завдання
Створити програму, яка знаходить суму всіх елементів у двовимірній матриці, використовуючи `NumPy`.

---

## Хід роботи

1.  Створено програму мовою Python 3 з використанням бібліотеки `numpy`.
2.  У програмі оголошено матрицю (багатовимірний масив `numpy.array`).
3.  За допомогою функції `numpy.sum()` знайдено та виведено на екран суму всіх елементів матриці.
4.  Програма успішно виконана, на екран виведено результат.
5.  Код розміщено у віддаленому репозиторії GitHub.

### 1) Код програми
[Переглянути код](./lb2.py)

### 2) Результат
![Результат роботи](./result.png)

---

## Висновки

Під час виконання цієї лабораторної роботи я створив(ла) програму на Python для знаходження суми всіх елементів у двовимірній матриці.

Я навчив(ла)ся:
* Імпортувати та використовувати сторонні бібліотеки, зокрема `numpy`.
* Створювати матриці (багатовимірні масиви) за допомогою `numpy.array()`.
* Використовувати вбудовану функцію `numpy.sum()` для швидкого та ефективного обчислення суми елементів матриці.